const connection = require('../connection/dbConnection');

const TABLE_USER = 'users';
const TABLE_SELLER_USER = 'seller_user';
const 


connection.connect((err) => {
    if(err){
        throw err;
    }
});